# 코드 품질 게이트 가이드 (초보자용)

이 문서는 이 템플릿에서 **코드를 작성 → 저장/커밋 → 검증**하는 과정에서 어떤 “품질 게이트(자동 검사)”가 동작하는지, 그리고 **어떤 파일에서 설정을 바꿀 수 있는지**를 초보자도 이해할 수 있게 설명합니다.

---

## 1) 한 줄 요약(이 템플릿의 품질 게이트는 무엇을 보장하나요?)

1. 코드가 “대충 돌아가는 수준”을 넘어서, 팀이 정한 규칙(포맷/린트/타입/스타일)을 자동으로 지키게 합니다.
2. 커밋할 때는 **바뀐 파일만 빠르게 자동 수정/검사**합니다(Husky + lint-staged).
3. 배포/PR 전에는 **전체 검증**을 한 번에 실행할 수 있습니다(`npm run validate`).

---

## 2) 먼저 알아야 하는 개념(초보자용)

### 2.1) 코드 품질 게이트(Code Quality Gate)

1. 한 줄 정의: “코드가 특정 기준을 만족하지 않으면 저장/커밋/배포 단계에서 통과하지 못하게 막는 검사”입니다.
2. 기능: 포맷(줄바꿈/따옴표/들여쓰기), 코드 규칙(사용하지 않는 변수), 타입 안정성(잘못된 타입), CSS 규칙(중복 셀렉터) 등을 검사합니다.
3. 목적: 팀 코드가 일관되게 유지되고, 실수를 조기에 잡게 합니다.
4. 왜 필요한지(리스크/사례):
   1. 사람마다 코딩 스타일이 달라 diff가 커지고 리뷰가 어려워집니다.
   2. 타입/린트 실수가 런타임 버그로 이어질 수 있습니다.
   3. CSS/SCSS가 커지면 중복/충돌이 늘어 유지보수가 어려워집니다.
5. 이 프로젝트에서 어디에 쓰이는지:
   1. 커밋 시: `.husky/pre-commit` → `lint-staged` 실행
   2. 수동 검증 시: `npm run validate` 실행
6. 짧은 예시(3~5줄):
   1. 사용하지 않는 변수를 만들면 ESLint가 에러/경고로 잡습니다.
7. 기대효과: “코드 스타일/품질을 사람 대신 자동으로 관리”해서 팀 생산성이 올라갑니다.
8. 성공 기준(어떻게 확인):
   1. `npm run validate`가 에러 없이 통과합니다.
   2. `git commit` 시 자동 검사/수정이 실행됩니다.

### 2.2) ESLint(자바스크립트/타입스크립트 린터) + Flat Config

1. 한 줄 정의: JS/TS 코드의 문제를 찾아주는 검사 도구입니다.
2. 기능:
   1. 문법/안전성/스타일 규칙을 검사합니다.
   2. 일부 규칙은 `--fix`로 자동 수정할 수 있습니다.
3. 목적: 실수(버그 가능성)를 리뷰 전에 자동으로 잡습니다.
4. 왜 필요한지(리스크/사례): “동작은 되지만 위험한 코드”가 쌓이면 유지보수가 급격히 어려워집니다.
5. 이 프로젝트에서 어디에 쓰이는지:
   1. 설정 파일: `eslint.config.js`(ESLint v9의 flat config 방식)
   2. 실행 스크립트: `npm run lint`, `npm run lint:fix`
   3. 커밋 훅: `lint-staged`가 JS/TS 파일에 `eslint --fix` 실행
6. 짧은 예시(3~5줄):
   1. `document.getElementById('root')!` 같은 강제 단언은 금지될 수 있습니다(규칙에 따라).
   2. Promise를 reject할 때 `Error`가 아닌 값을 던지면 에러가 날 수 있습니다.
7. 기대효과: 코딩 습관/실수 유형이 표준화되고, 버그가 줄어듭니다.
8. 성공 기준:
   1. `npm run lint`가 통과합니다.
   2. `npm run lint:fix`로 자동 수정이 수행됩니다.

### 2.3) Type-aware lint(타입 정보를 사용하는 ESLint)

1. 한 줄 정의: ESLint가 TypeScript의 타입 정보를 실제로 읽고 더 강하게 검사하는 방식입니다.
2. 기능: “타입을 알아야만 잡을 수 있는 문제”를 더 잘 잡습니다.
3. 목적: 런타임 전에 타입 기반 버그를 줄입니다.
4. 왜 필요한지(리스크/사례):
   1. 타입을 모르면 ESLint가 놓치는 버그 유형이 있습니다.
   2. 대신 타입 정보를 쓰면 린트가 느려질 수 있습니다(비용).
5. 이 프로젝트에서 어디에 쓰이는지:
   1. `eslint.config.js`의 `parserOptions.project`가 `tsconfig.app.json`, `tsconfig.node.json`을 가리킵니다.
   2. `typescript-eslint`의 `strictTypeChecked` 구성을 사용합니다.
6. 짧은 예시(3~5줄):
   1. “이 값이 null일 수 있는지” 같은 판단이 더 정확해집니다.
7. 기대효과: 타입 안전성이 강화되어 배포 후 버그가 줄어듭니다.
8. 성공 기준:
   1. `npm run lint` 실행 시 타입 기반 규칙이 실제로 적용됩니다.

### 2.4) Prettier(코드 포맷터)

1. 한 줄 정의: 코드를 “보기 좋게” 자동 정렬하는 도구입니다.
2. 기능: 들여쓰기/따옴표/줄바꿈/콤마 등을 일관되게 만듭니다.
3. 목적: 포맷 논쟁을 없애고 diff를 줄입니다.
4. 왜 필요한지(리스크/사례): 포맷이 제각각이면 리뷰에서 “의미 없는 변경”이 늘어납니다.
5. 이 프로젝트에서 어디에 쓰이는지:
   1. 설정 파일: `.prettierrc`
   2. 실행 스크립트: `npm run format`, `npm run format:check`
   3. 주의: 이 템플릿은 JS/TS 포맷을 **ESLint를 통해서도 강제**합니다(`eslint-plugin-prettier`).
6. 짧은 예시(3~5줄):
   1. 줄 길이 100 기준으로 자동 줄바꿈이 적용됩니다.
7. 기대효과: 모든 파일이 비슷한 스타일로 정리되어 읽기 쉽습니다.
8. 성공 기준:
   1. `npm run format:check`가 통과합니다.

### 2.5) Stylelint(CSS/SCSS 린터)

1. 한 줄 정의: CSS/SCSS 파일의 문제를 찾아주는 검사 도구입니다.
2. 기능: 문법 오류, 중복 셀렉터, 네이밍 규칙, 일부 스타일 규칙을 검사/수정합니다.
3. 목적: CSS 품질을 유지하고, 유지보수하기 쉬운 스타일 코드를 만듭니다.
4. 왜 필요한지(리스크/사례): CSS는 규모가 커질수록 충돌/중복이 늘어 디버깅이 어려워집니다.
5. 이 프로젝트에서 어디에 쓰이는지:
   1. 설정 파일: `stylelint.config.js`
   2. 실행 스크립트: `npm run lint:style`, `npm run lint:style:fix`
   3. 커밋 훅: `lint-staged`가 CSS/SCSS 파일에 `stylelint --fix` + `prettier --write` 실행
6. 짧은 예시(3~5줄):
   1. 동일한 셀렉터를 파일 안에서 중복 정의하면 에러가 납니다.
7. 기대효과: 스타일 코드가 일관되고 중복이 줄어듭니다.
8. 성공 기준:
   1. `npm run lint:style`가 통과합니다.

### 2.6) Husky + lint-staged(커밋 전에 자동 검사/수정)

1. 한 줄 정의: Git 커밋 시점에 자동으로 검사/수정을 실행하는 장치입니다.
2. 기능:
   1. Husky: “커밋 전에 이 스크립트를 실행해!” 같은 Git hook을 관리합니다.
   2. lint-staged: “이번 커밋에 포함되는 파일(staged)만 골라서 빠르게 검사/수정”합니다.
3. 목적: “커밋되는 코드”의 품질을 자동으로 보장합니다.
4. 왜 필요한지(리스크/사례):
   1. 커밋이 쌓이고 나서 한 번에 고치면 비용이 큽니다.
   2. 커밋 단위로 고치면 diff가 작고 리뷰가 쉽습니다.
5. 이 프로젝트에서 어디에 쓰이는지:
   1. 훅 스크립트: `.husky/pre-commit`
   2. lint-staged 설정: `package.json`의 `"lint-staged"`
   3. Husky 설치 트리거: `package.json`의 `scripts.prepare = "husky"`
6. 짧은 예시(3~5줄):
   1. `git add`로 파일을 stage한 뒤 `git commit`하면, 자동으로 ESLint/Stylelint/Prettier가 실행됩니다.
7. 기대효과: 커밋이 “항상 일정한 품질”을 유지합니다.
8. 성공 기준:
   1. `git commit` 할 때 검사 로그가 뜨고, 실패 시 커밋이 중단됩니다.

---

## 3) 이 템플릿에서 실제로 실행되는 “품질 게이트” 목록

### 3.1) 커밋 시점(Pre-commit)

1. 실행 경로
   1. `git commit` 실행
   2. Husky가 `.husky/pre-commit` 실행
   3. `.husky/pre-commit`가 `npx lint-staged` 실행
2. lint-staged가 파일 종류별로 실행하는 작업(`package.json` 기준)
   1. `*.{js,jsx,ts,tsx}`: `eslint --fix`
   2. `*.{css,scss}`: `stylelint --fix` → `prettier --write`
   3. `*.{json,md}`: `prettier --write`
3. 중요한 포인트(중복 실행 축소)
   1. JS/TS 파일은 Prettier를 따로 돌리지 않습니다.
   2. 이유: 이 템플릿은 `eslint-plugin-prettier`로 “Prettier 포맷을 ESLint에서 에러로 강제”하고, `eslint --fix`로 자동 포맷까지 처리합니다.
4. 커밋 중단/수정 흐름(초보자용)
   1. 커밋이 실패하면: 터미널 출력에서 어떤 파일/규칙이 실패했는지 확인합니다.
   2. 자동 수정이 일어난 경우: 수정된 파일을 다시 `git add`로 stage한 뒤 커밋을 다시 시도합니다.

### 3.2) 수동 검증(전체 게이트)

1. 전체 검증 명령(권장): `npm run validate`
2. 이 명령이 하는 일(`package.json` 기준)
   1. `npm run format:check`: Prettier 포맷이 맞는지 검사(수정은 안 함)
   2. `npm run typecheck`: TypeScript 타입 체크
   3. `npm run lint`: ESLint 검사(전체 소스)
   4. `npm run lint:style`: Stylelint 검사(전체 스타일)
3. 언제 쓰면 좋은가(초보자 기준)
   1. 큰 작업을 마무리할 때
   2. PR 올리기 전(또는 배포 전)

---

## 4) 설정 파일 위치(“어디를 고치면 규칙이 바뀌나요?”)

1. ESLint 설정: `eslint.config.js`
2. Prettier 설정: `.prettierrc`
3. Stylelint 설정: `stylelint.config.js`
4. Husky 훅: `.husky/pre-commit`
5. lint-staged 규칙: `package.json`의 `"lint-staged"`
6. 전체 검증 스크립트: `package.json`의 `"scripts"`
7. 에디터(저장 시 자동 포맷/자동 수정): `.vscode/settings.json`, `.vscode/extensions.json`
8. 줄바꿈/들여쓰기 기본값(에디터 공통): `.editorconfig`
9. 생성 캐시 무시(커밋 제외): `.gitignore`의 `.stylelintcache` 등

---

## 5) 초보자 추천 워크플로(실수 적게 하는 순서)

### 5.1) 시작(처음 클론했을 때)

1. 의존성 설치: `npm ci`(권장) 또는 `npm install`
2. Husky가 제대로 연결됐는지 확인(선택)
   1. `git config --get core.hooksPath` 실행
   2. 출력이 `.husky`면 정상입니다.
3. 개발 서버 실행: `npm run dev`

### 5.2) 개발 중(저장할 때)

1. VS Code에서 이 템플릿 폴더를 열면, 권장 확장 설치를 제안합니다(`.vscode/extensions.json`).
2. `Ctrl/⌘ + S`로 저장하면 Prettier 포맷이 적용되도록 설정되어 있습니다(`.vscode/settings.json`).
3. 저장 시 ESLint/Stylelint 자동 수정도 함께 쓰고 싶으면, ESLint/Stylelint 확장을 설치한 상태에서 동작하는지 확인합니다.

### 5.3) 커밋할 때

1. 변경 파일을 스테이징: `git add -A`
2. 커밋: `git commit -m "설명"`
3. 커밋이 실패하면
   1. 출력 메시지에서 실패한 규칙/파일을 확인합니다.
   2. 자동 수정이 반영되었을 수 있으니 `git status`로 변경 여부를 확인합니다.
   3. 수정된 파일이 있으면 다시 `git add -A` 후 커밋을 재시도합니다.

### 5.4) PR/배포 전(전체 검사)

1. `npm run validate` 실행
2. 실패하면
   1. 포맷 문제: `npm run format`
   2. ESLint 자동 수정: `npm run lint:fix`
   3. Stylelint 자동 수정: `npm run lint:style:fix`
   4. 타입 문제: 에러 메시지에 나온 파일/라인을 수정

---

## 6) 자주 마주치는 규칙과 대응(초보자용)

### 6.1) “`!`(non-null assertion) 쓰지 마세요” 에러가 날 때

1. 원인/리스크/기대효과
   1. 원인: `something!`는 “무조건 값이 있다”고 강제하는 문법입니다.
   2. 리스크: 실제로 값이 없으면 런타임에서 터질 수 있습니다.
   3. 기대효과: 런타임 가드를 쓰면 안전해집니다.
2. 해결 절차
   1. 값을 가져온 뒤 `if (!value) throw new Error(...)` 형태로 가드합니다.
3. 성공 기준
   1. ESLint가 더 이상 해당 에러를 내지 않습니다.

### 6.2) “`any` 쓰지 마세요” 경고가 날 때

1. 원인/리스크/기대효과
   1. 원인: `any`는 타입 검사를 포기하는 타입입니다.
   2. 리스크: 타입 안전성이 무너져 버그가 숨을 수 있습니다.
   3. 기대효과: `unknown` + 런타임 가드로 안전하게 처리할 수 있습니다.
2. 해결 절차
   1. 정말 모르는 값이면 `unknown`을 사용합니다.
   2. 사용할 때는 `typeof`, `in` 연산자 등으로 안전하게 좁혀서 사용합니다.
3. 성공 기준
   1. 타입 체크와 ESLint 경고가 줄어듭니다.

### 6.3) “CSS/SCSS에서 duplicate selector” 에러가 날 때

1. 원인/리스크/기대효과
   1. 원인: 같은 선택자(예: `button`, `a`)가 파일 안에서 중복 선언됐습니다.
   2. 리스크: 어떤 스타일이 최종 적용되는지 추적이 어려워집니다.
   3. 기대효과: 선언을 한 곳으로 모으면 유지보수가 쉬워집니다.
2. 해결 절차
   1. 중복 선언을 삭제하거나 한 곳으로 통합합니다.
3. 성공 기준
   1. `npm run lint:style`가 통과합니다.

---

## 7) 트러블슈팅(초보자용)

### 7.1) “커밋할 때 자동 검사가 실행되지 않아요”

1. 원인/리스크/기대효과
   1. 원인: Husky가 설치/연결되지 않았을 수 있습니다.
   2. 리스크: 품질 게이트가 무력화되어 품질이 흔들릴 수 있습니다.
   3. 기대효과: 연결을 복구하면 커밋 품질이 보장됩니다.
2. 해결 절차
   1. `npm ci` 또는 `npm install`을 다시 실행해 `prepare`가 동작하는지 확인합니다.
   2. `git config --get core.hooksPath`가 `.husky`를 반환하는지 확인합니다.
   3. `.husky/pre-commit`가 존재하고 실행 가능한지 확인합니다.
3. 성공 기준
   1. 커밋 시 `lint-staged`가 실행됩니다.

### 7.2) “`npm run validate`는 통과하는데 커밋이 실패해요(또는 반대)”

1. 원인/리스크/기대효과
   1. 원인: 커밋 훅은 staged 파일만 검사하고, `validate`는 전체를 검사합니다.
   2. 리스크: “일부 파일만” 남몰래 깨질 수 있습니다.
   3. 기대효과: 목적에 맞게 둘 다 사용하면 안전합니다.
2. 해결 절차
   1. 커밋 직전에는 훅으로 빠르게 잡고, 큰 변경 후에는 `npm run validate`로 전체를 확인합니다.
3. 성공 기준
   1. 커밋과 전체 검증이 모두 안정적으로 통과합니다.

---

## 8) 마지막 체크(템플릿의 성공 기준)

1. `npm run validate`가 에러 없이 통과합니다.
2. `git commit` 시 자동 검사/수정이 실행됩니다.
3. VS Code에서 저장 시 포맷이 자동 적용됩니다(확장 설치 포함).
