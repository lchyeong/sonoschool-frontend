# CSR 템플릿 구현 가이드 (Vite + React + TypeScript + npm)

작성일: 2026-01-29  
대상: 이 레포를 “React CSR 보일러플레이트/템플릿”으로 표준화해 반복 재사용하려는 개발자

---

## 1) 목표(원인/리스크/기대효과)

1. 목표
   1. 이 레포를 “대부분의 React CSR 프로젝트에서 바로 가져다 쓸 수 있는 기본 템플릿”으로 정리합니다.
   2. 템플릿 사용자가 옵션을 고르지 않아도(선택 단계 없음) **기본 설치/기본 구성만으로** 라우팅, 데이터 패칭, 폼, 목킹, 테스트, 에러/로딩 UX, 품질 게이트가 동작하도록 합니다.
   3. 단, 내부 구현은 “모듈 단위로 분리”해 유지보수와 확장/제거가 쉬운 구조를 목표로 합니다(구조는 모듈형, 제공은 기본 포함).
2. 리스크(템플릿화에서 자주 발생)
   1. 기능을 많이 넣다가 의존성이 비대해져 초기 복잡도가 올라갑니다.
   2. “서버 상태(데이터) vs 클라이언트 상태(UI)” 경계가 불명확하면 전역 상태 남용/캐시 불일치가 생깁니다.
   3. 에러/로딩/비동기 취소 규칙이 없으면 동일한 버그가 반복됩니다.
   4. 테스트/목킹이 없으면 리팩터링 비용이 급증합니다.
3. 기대효과
   1. 신규 프로젝트 셋업 시간을 단축합니다(라우팅/검증/테스트/목킹/품질 규칙이 기본값으로 존재).
   2. 런타임 안정성(에러 경계, 표준 API 에러 형태), 비동기 안정성(중복 요청/레이스 최소화), 타입 안정성을 동시에 올립니다.
   3. 팀 합의가 필요한 영역(폴더 구조/레이어/규칙)을 템플릿 레벨에서 고정해 리뷰 비용을 줄입니다.

---

## 2) 현재 레포에서 확인한 사실(전제)

1. 빌드/런타임
   1. Vite 사용(`vite.config.ts`)
   2. TypeScript 사용(`tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`)
   3. npm 사용(`package-lock.json` 존재)
2. 주요 라이브러리(2026-01-29 기준 `package.json`)
   1. React 19(`react`, `react-dom`)
   2. React Router(`react-router-dom`)
   3. Axios(`axios`)
   4. Zustand(`zustand`)
3. 품질 게이트(이미 기본 제공)
   1. ESLint/Prettier/Stylelint/Husky/lint-staged가 설정되어 있습니다(`CODE_QUALITY_GATE_GUIDE.md` 참고).
   2. 기본 스크립트는 `npm run validate`로 포맷/타입/린트/스타일 린트를 일괄 검증합니다(`package.json`).
4. 스타일 시스템(이미 기본 제공)
   1. SCSS + CSS Modules + 토큰/믹스인이 구성되어 있습니다(`STYLES_BOILERPLATE_GUIDE.md` 참고).
5. 환경변수(이미 일부 토글 존재)
   1. `.env*`에 `VITE_ENABLE_MOCK`, `VITE_ENABLE_ANALYTICS`, `VITE_API_URL` 등이 존재합니다.
6. 현재 앱 상태
   1. `src/main.tsx`는 `App`을 렌더링하지만 라우팅/프로바이더 구성은 아직 최소 상태입니다.

---

## 3) 먼저 알아야 하는 개념(핵심만)

### 3.1) “코어 + 모듈” 템플릿(하지만 기본 포함)

1. 한 줄 정의: “앱을 구성하는 기능을 모듈 단위로 분리하되, 템플릿에는 전부 기본으로 포함시키는” 방식입니다.
2. 기능
   1. 폴더/레이어 경계를 통해 관심사를 분리합니다(라우팅, API, 폼, 테스트, 목킹 등).
   2. 각 모듈은 독립적으로 교체/삭제가 가능하도록 인터페이스(사용 지점)를 최소화합니다.
3. 목적
   1. 템플릿 재사용성(기본값은 풍부)과 유지보수성(구조는 분리)을 동시에 확보합니다.
4. 왜 필요한지(문제/리스크/사례)
   1. “기능을 많이 넣은 단일 구조”는 시간이 지나면 서로 강결합되어 수정이 어려워집니다.
   2. 반대로 “너무 미니멀한 템플릿”은 매 프로젝트마다 같은 구성(테스트/목킹/에러 처리)을 다시 만들게 됩니다.
5. 이 프로젝트에서 어디에 쓰이는지
   1. `src/app`(App Shell), `src/routes`(라우팅), `src/api`(HTTP), `src/mocks`(MSW), `src/test`(테스트 유틸)처럼 역할별 디렉토리로 반영합니다.
6. 짧은 예시(3~5줄)
   1. 라우터는 `src/routes/router.tsx`에만 정의하고, 앱 엔트리는 `src/app/AppProviders.tsx`에서 한 번만 라우터를 주입합니다.
7. 기대효과
   1. 기본 제공 기능을 풍부하게 유지하면서도, 프로젝트에 맞게 일부 기능을 제거/교체하는 비용이 낮아집니다.
8. 성공 기준
   1. “한 모듈”을 바꿀 때 다른 모듈을 광범위하게 수정하지 않아도 됩니다(변경 영향 범위가 예측 가능).

### 3.2) App Shell(앱 껍데기)

1. 한 줄 정의: 라우터/프로바이더/전역 UI(레이아웃, 토스트 등)를 묶어 앱 전체의 “기본 골격”을 만드는 계층입니다.
2. 기능: Provider 조합, 전역 에러 경계, 전역 로딩/토스트 UI, 레이아웃.
3. 목적: 앱 전반 규칙(에러/로딩/컨텍스트)을 한 곳에서 일관되게 적용합니다.
4. 왜 필요한지: Provider가 분산되면(여러 파일에 흩어짐) 순서/설정이 깨져 디버깅이 어려워집니다.
5. 이 프로젝트에서 어디에 쓰이는지: `src/main.tsx`는 최소화하고, `src/app/*`에서 App Shell을 구성합니다.
6. 짧은 예시(3~5줄)
   1. `<QueryClientProvider>` + `<RouterProvider>` + `<ErrorBoundary>`를 `AppProviders`로 묶습니다.
7. 기대효과: 전역 동작이 예측 가능해지고 신규 기능 추가가 쉬워집니다.
8. 성공 기준: 라우팅/데이터패칭/토스트/에러처리가 어디서 설정되는지 1분 내 찾을 수 있습니다.

### 3.3) Server State(서버 상태) vs Client State(클라이언트 상태)

1. 한 줄 정의
   1. Server State: 서버에서 가져오는 데이터(캐시, 갱신, 동기화가 핵심)
   2. Client State: UI 상태(모달 열림, 필터 값 등, 앱 내부에서만 의미)
2. 기능
   1. Server State는 캐싱/리트라이/중복요청 제거가 중요합니다.
   2. Client State는 UI 상호작용과 렌더링 최적화가 중요합니다.
3. 목적: 데이터/상태의 책임을 분리해 버그와 복잡도를 줄입니다.
4. 왜 필요한지(사례)
   1. 서버 데이터를 전역 스토어에 넣으면 “언제 새로고침/동기화할지” 규칙이 붕괴합니다.
5. 이 프로젝트에서 어디에 쓰이는지
   1. Server State: TanStack Query(도입 예정) 또는 동등한 쿼리 캐시 계층.
   2. Client State: Zustand(`src/stores`) + React state.
6. 짧은 예시(3~5줄)
   1. “사용자 목록”은 Server State로 캐시합니다.
   2. “목록 정렬 방식”은 Client State로 저장합니다.
7. 기대효과: 불필요한 렌더링/중복요청이 줄고 데이터 일관성이 좋아집니다.
8. 성공 기준: 서버 데이터는 “단일 쿼리 계층”으로, UI 상태는 “스토어/컴포넌트 상태”로 자연스럽게 분리됩니다.

### 3.4) MSW(Mock Service Worker)

1. 한 줄 정의: 브라우저에서 네트워크 요청을 가로채 “가짜 API 응답”을 제공하는 목킹 도구입니다.
2. 기능: 개발/테스트 환경에서 백엔드 없이도 API 시나리오를 재현합니다.
3. 목적: 개발/테스트를 백엔드 상태에 의존하지 않게 합니다.
4. 왜 필요한지: 백엔드가 불안정/미완성이면 프론트 개발이 멈추고, 테스트는 플래키(불안정)해집니다.
5. 이 프로젝트에서 어디에 쓰이는지: `VITE_ENABLE_MOCK=true`일 때 개발 서버에서 MSW를 자동 활성화합니다.
6. 짧은 예시(3~5줄)
   1. `/contact` POST 요청에 대해 성공/실패 케이스를 스위칭해서 프론트 UX를 검증합니다.
7. 기대효과: UI 개발 속도 향상, 테스트 안정성 향상.
8. 성공 기준: 백엔드가 꺼져 있어도 “핵심 화면” 개발/테스트가 가능합니다.

### 3.5) E2E 테스트(End-to-End)

1. 한 줄 정의: 실제 브라우저에서 “사용자 동작(클릭/입력/라우팅)”을 통째로 검증하는 테스트입니다.
2. 기능: 라우팅/네트워크/렌더링/폼 제출 등 전체 흐름을 검증합니다.
3. 목적: 배포 전 치명적인 회귀(핵심 플로우 깨짐)를 잡습니다.
4. 왜 필요한지: 단위 테스트만으로는 라우팅/비동기/브라우저 호환 이슈를 놓치기 쉽습니다.
5. 이 프로젝트에서 어디에 쓰이는지: Playwright 기반으로 `npm run e2e`를 제공하는 구성을 목표로 합니다.
6. 짧은 예시(3~5줄)
   1. “홈 → 문의 폼 → 제출 성공 토스트” 시나리오를 브라우저에서 재현합니다.
7. 기대효과: 릴리즈 품질 향상, 회귀 비용 감소.
8. 성공 기준: CI에서 브라우저 기반 핵심 시나리오 1~2개가 안정적으로 통과합니다.

---

## 4) 구현 방식 3가지(전제는 B, 본 문서는 B 기준)

1. 방식 A: “미니멀 코어”만 제공
   1. 장점: 의존성/복잡도 최소.
   2. 단점: 실제 프로젝트에서 필요한 것(목킹/테스트/폼/서버상태)을 매번 다시 추가하게 될 가능성이 큽니다.
2. 방식 B: “코어 + 모듈”을 구조적으로 분리하되 **기본 포함**
   1. 장점: 재사용성이 높고, 구조가 모듈형이라 유지보수/확장/부분 제거가 쉽습니다.
   2. 단점: 초기 문서/가이드 품질이 중요합니다(어디에 뭘 둬야 하는지 명확해야 함).
3. 방식 C: 코드 생성기(스캐폴더)까지 제공
   1. 장점: 옵션 조합/프로젝트 생성 자동화까지 가능.
   2. 단점: 유지보수 비용이 가장 큽니다(옵션 조합 테스트 필요).

---

## 5) 템플릿 “기본 제공” 모듈 목록(옵션 선택 없음)

1. 코어(항상 쓰이는 기반)
   1. App Shell(Provider 조합 + 전역 UX)
   2. 라우팅(React Router) + 레이아웃 + 404/에러 페이지
   3. API 레이어(Axios 인스턴스 + 에러 표준화 + 취소/타임아웃 규칙)
   4. 스타일 시스템(SCSS + CSS Modules + 토큰/믹스인)과 공통 UI 프리미티브
   5. 품질 게이트(ESLint/Prettier/Stylelint/Typecheck) + 커밋 훅(Husky)
2. 모듈(기본 포함, 미사용 가능)
   1. Server State 관리(TanStack Query 권장)
   2. 폼/검증(React Hook Form + Zod 권장)
   3. API 목킹(MSW)
   4. 테스트(단위/컴포넌트: Vitest + React Testing Library)
   5. E2E 테스트(Playwright)
   6. 관측성(에러 로깅/트래킹 연동 지점: 예, Sentry)과 런타임 로그 정책
   7. 접근성(A11y) 기본값(포커스/키보드 내비게이션/대비) 체크리스트

---

## 6) 권장 폴더 구조(모듈형이지만 기본 포함)

1. 목표
   1. “어디에 뭘 둬야 하는지”를 초보자도 즉시 판단할 수 있게 합니다.
   2. 라우팅/데이터/상태/컴포넌트/테스트 유틸을 분리합니다.
2. 권장 구조(예시)
   1. 아래는 “최종 목표 구조” 예시입니다.
   2. 현재 레포에 이미 존재하는 디렉토리(`src/api`, `src/styles`, `src/stores` 등)는 가능한 재사용하고, 필요한 경우 점진적으로 이동합니다.

```txt
src/
  app/
    AppProviders.tsx
    ErrorBoundary.tsx
  routes/
    router.tsx
    routePaths.ts
  pages/
    HomePage/
      HomePage.tsx
      HomePage.module.scss
    NotFoundPage/
      NotFoundPage.tsx
  api/
    axiosInstance.ts
    errors.ts
    http.ts
    contact.ts
  query/
    queryClient.ts
    queryKeys.ts
  forms/
    schemas/
      contactSchema.ts
  components/
    ui/
      Button/
      TextField/
    feedback/
      Toast/
      Loading/
  mocks/
    browser.ts
    handlers.ts
  test/
    setup.ts
    renderWithProviders.tsx
  stores/
    useAppStore.ts
  utils/
    assert.ts
    format.ts
  types/
    api.ts
main.tsx
```

3. 핵심 규칙(의존 방향)
   1. `pages`는 `api/query/components`를 사용할 수 있습니다.
   2. `api`는 `pages`를 참조하면 안 됩니다(역방향 결합 금지).
   3. `components/ui`는 “비즈니스 도메인”을 몰라야 합니다(재사용 가능해야 함).
   4. `test` 유틸은 프로덕션 코드에 섞이지 않게 분리합니다.

---

## 7) 단계별 구현 절차(이 문서대로 하면 템플릿이 완성되는 순서)

### 7.1) 1단계: 의존성 추가 계획 수립(기본 포함 모듈을 위한 패키지)

1. 원인/리스크/기대효과
   1. 원인: 현재는 라우팅/상태/axios는 있으나, “서버 상태/폼 검증/목킹/테스트/E2E”는 기본 구성으로 보이지 않습니다.
   2. 리스크: 패키지를 무분별하게 추가하면 번들 크기/학습 난이도가 올라갑니다.
   3. 기대효과: “대부분의 CSR 프로젝트에서 반복되는 기능”을 기본값으로 제공할 수 있습니다.
2. 해결 절차(패키지 후보)
   1. Server State(TanStack Query)
      1. `@tanstack/react-query`
      2. 필요 시: `@tanstack/react-query-devtools`(개발 전용)
   2. Form + Validation
      1. `react-hook-form`
      2. `zod`
      3. `@hookform/resolvers`
   3. Mocking
      1. `msw`
   4. Unit/Component Test
      1. `vitest`
      2. `@testing-library/react`
      3. `@testing-library/jest-dom`
      4. `jsdom`(테스트 환경)
   5. E2E
      1. `@playwright/test`
   6. (선택이지만 템플릿 기본 포함 권장) 접근성/품질 보강
      1. `eslint-plugin-testing-library`, `eslint-plugin-jest-dom`(테스트 관련 린트)
3. 검증(성공 기준)
   1. `package.json`에 새 스크립트(`test`, `e2e` 등) 추가 후, 로컬에서 실행 가능해야 합니다.
   2. “템플릿 기본 설치 직후” 테스트/목킹 설정 파일이 존재해야 합니다(아직 테스트가 0개여도 실행 자체는 가능해야 함).

### 7.2) 2단계: App Shell 구성(Provider를 한 곳에 고정)

1. 원인/리스크/기대효과
   1. 원인: Provider/라우터/에러 경계가 분산되면 설정이 깨지기 쉽습니다.
   2. 리스크: Provider 순서가 바뀌면(예: QueryClientProvider 누락) 런타임 오류가 납니다.
   3. 기대효과: 앱 전역 동작(라우팅/데이터/토스트/에러)을 한 파일에서 추적할 수 있습니다.
2. 해결 절차(권장 파일)
   1. `src/app/AppProviders.tsx` 생성
      1. 포함: `RouterProvider`, `QueryClientProvider`, 전역 `ErrorBoundary`, 전역 토스트(도입 시)
   2. `src/main.tsx`는 “렌더링 진입점”만 남기고 App Shell로 위임
3. 검증(성공 기준)
   1. `src/main.tsx`를 보면 “앱이 어떤 Provider를 쓰는지”를 10초 내 추적할 수 있어야 합니다.

### 7.3) 3단계: 라우팅 표준화(React Router)

1. 원인/리스크/기대효과
   1. 원인: CSR 템플릿은 “페이지 추가”가 가장 자주 일어나는 작업입니다.
   2. 리스크: 라우팅이 즉흥적으로 늘어나면 404/에러/레이아웃 규칙이 깨집니다.
   3. 기대효과: 페이지 추가가 표준 절차로 고정됩니다.
2. 해결 절차(권장 구성)
   1. `src/routes/routePaths.ts`에 경로 상수(예: `/`, `/contact`) 고정
   2. `src/routes/router.tsx`에서 `createBrowserRouter` 기반 라우터 구성
   3. `pages`는 폴더 단위로 구성(`HomePage/HomePage.tsx` 등)
   4. 라우트 단위 코드 스플리팅
      1. `React.lazy` 또는 React Router의 lazy route 패턴 중 1개로 표준화
3. 검증(성공 기준)
   1. 새 페이지 추가가 “`pages`에 컴포넌트 추가 + router 등록”으로 끝납니다.
   2. 존재하지 않는 경로에서 404 페이지가 렌더링됩니다.

### 7.4) 4단계: 에러/로딩 UX 표준화(전역 + 라우트 단위)

1. 원인/리스크/기대효과
   1. 원인: CSR은 런타임 예외가 곧 UX 붕괴로 이어집니다.
   2. 리스크: 페이지 단위로 각자 에러 처리하면 사용자 경험이 불일치합니다.
   3. 기대효과: “실패했을 때 어떻게 보일지”가 템플릿 기본값으로 통일됩니다.
2. 해결 절차(권장 구성)
   1. 전역 Error Boundary
      1. `src/app/ErrorBoundary.tsx`(react-error-boundary 같은 라이브러리 사용 또는 직접 구현)
   2. 라우트 에러 페이지
      1. `errorElement` 또는 router error handling으로 표준 페이지 제공
   3. 로딩 UI
      1. `components/feedback/Loading`에 최소 1개 로딩 컴포넌트 제공
3. 검증(성공 기준)
   1. 의도적으로 에러를 발생시켰을 때 “흰 화면”이 아니라 폴백 UI가 보입니다.

### 7.5) 5단계: 환경변수 사용 규칙(런타임 검증 + 단일 진입점)

1. 원인/리스크/기대효과
   1. 원인: Vite 환경변수는 기본적으로 문자열이며, 값 누락은 런타임까지 발견되지 않을 수 있습니다.
   2. 리스크: 프로덕션에서 “환경변수 누락”으로 앱이 조용히 망가질 수 있습니다.
   3. 기대효과: 앱 시작 시점에 빠르게 실패(fail-fast)해서 문제를 즉시 발견합니다.
2. 해결 절차(권장 구성)
   1. `src/config/env.ts` 생성
      1. `import.meta.env`를 읽고, `zod`로 스키마 검증 후 “정규화된 env 객체”만 export
      2. 예: `VITE_API_URL`은 URL 형식 검증, `VITE_ENABLE_MOCK`은 boolean으로 파싱
   2. 앱 전역에서 `import.meta.env` 직접 접근 금지(규칙/컨벤션)
3. 검증(성공 기준)
   1. 필수 env 누락 시 개발 서버 실행 단계에서 명확한 에러 메시지가 출력됩니다.

### 7.6) 6단계: API 레이어 표준화(Axios)

1. 원인/리스크/기대효과
   1. 원인: 컴포넌트마다 axios 호출이 흩어지면 에러 형태/취소/인증 처리 규칙이 깨집니다.
   2. 리스크: 중복 요청, race condition, 에러 메시지 불일치가 발생합니다.
   3. 기대효과: 네트워크 정책이 한 곳에서 통제됩니다.
2. 해결 절차(권장 구성)
   1. `src/api/axiosInstance.ts`를 단일 인스턴스로 유지(현재 파일을 확장)
      1. timeout, baseURL, 공통 헤더, 응답 에러 정규화 유지
      2. AbortController 기반 취소를 사용할 규칙 정의(axios v1 지원)
   2. 에러 타입 표준화
      1. `src/api/errors.ts`에 “사용자 메시지/개발자 메시지/원인”을 포함한 에러 형태 정의
   3. endpoint 파일 분리
      1. `src/api/*`에 도메인별 파일(예: `contact.ts`, `auth.ts`) 유지
3. 검증(성공 기준)
   1. UI는 “API 함수”만 호출하고 axios 인스턴스에 직접 접근하지 않아도 됩니다.
   2. 에러 메시지는 사용자 친화적인 형태로 일관됩니다.

### 7.7) 7단계: Server State 표준화(TanStack Query)

1. 원인/리스크/기대효과
   1. 원인: CSR에서 서버 데이터는 캐시/동기화 전략이 없으면 중복 요청과 불일치가 반복됩니다.
   2. 리스크: useEffect로 직접 패칭하면 취소/중복/리트라이/로딩 상태가 각자 구현되어 유지보수 비용이 폭증합니다.
   3. 기대효과: 캐시/리트라이/중복요청 제거가 표준화됩니다.
2. 해결 절차(권장 구성)
   1. `src/query/queryClient.ts` 생성
      1. 기본 옵션(예: retry 조건, staleTime, refetchOnWindowFocus) 표준화
   2. `src/query/queryKeys.ts`로 쿼리 키 규칙 고정
   3. `src/app/AppProviders.tsx`에 `QueryClientProvider` 주입
   4. API 함수는 `src/api/*`, 쿼리 훅은 `src/query/*`에 배치하는 규칙 유지
3. 검증(성공 기준)
   1. 동일 데이터 조회가 중복 요청 없이 캐시로 동작합니다.
   2. 로딩/에러/성공 상태가 UI에서 일관되게 표현됩니다.

### 7.8) 8단계: Client State 표준화(Zustand)

1. 원인/리스크/기대효과
   1. 원인: “UI 상태”도 커지면 기준 없이 저장 위치가 분산됩니다.
   2. 리스크: 전역 스토어가 비대해지고 테스트가 어려워집니다.
   3. 기대효과: 전역 상태의 책임이 명확해져 유지보수성이 올라갑니다.
2. 해결 절차(권장 구성)
   1. `src/stores`에 스토어를 “도메인/기능 기준”으로 분리
   2. 서버 데이터는 Zustand에 저장하지 않는 원칙 유지(3.3 참고)
   3. persist가 필요하면 “저장소(localStorage 등) 보안 리스크”를 문서화하고 최소화
3. 검증(성공 기준)
   1. UI 상태(예: 테마, 사이드바 열림)는 Zustand로 일관되게 관리됩니다.

### 7.9) 9단계: 폼/검증 표준화(React Hook Form + Zod)

1. 원인/리스크/기대효과
   1. 원인: 폼은 대부분의 CSR에서 필수이며, 검증 로직이 흩어지기 쉽습니다.
   2. 리스크: 런타임에서만 발견되는 입력 오류, UX 불일치, 타입 불일치가 생깁니다.
   3. 기대효과: 폼 구현 속도와 안정성이 올라갑니다.
2. 해결 절차(권장 구성)
   1. 스키마를 `src/forms/schemas`에 고정
   2. 스키마 기반으로 타입 추론(Zod → TS 타입) 규칙 사용
   3. 폼 컴포넌트는 `components/ui`의 입력 컴포넌트를 재사용하도록 구성
3. 검증(성공 기준)
   1. 잘못된 입력에서 일관된 에러 메시지와 포커스/ARIA 처리가 적용됩니다.

### 7.10) 10단계: API 목킹 표준화(MSW + env 토글)

1. 원인/리스크/기대효과
   1. 원인: 현재 `.env.development`에 `VITE_ENABLE_MOCK=true`가 이미 존재합니다.
   2. 리스크: 목킹이 수동이면 팀마다 켜는 방법이 달라져 혼란이 생깁니다.
   3. 기대효과: 개발/테스트 환경에서 일관되게 목킹을 켜고 끌 수 있습니다.
2. 해결 절차(권장 구성)
   1. `src/mocks/browser.ts`, `src/mocks/handlers.ts` 구성
   2. 개발 환경에서만 `VITE_ENABLE_MOCK`를 확인해 worker를 start
   3. 초기에는 “핵심 API 1~2개”만 핸들러로 제공(예: contact)
3. 검증(성공 기준)
   1. 백엔드 없이도 개발 서버에서 API 응답 시나리오를 재현할 수 있습니다.

### 7.11) 11단계: 단위/컴포넌트 테스트 표준화(Vitest + RTL)

1. 원인/리스크/기대효과
   1. 원인: 품질 게이트는 있으나, UI 동작을 보장하는 테스트 러너/유틸은 기본으로 보이지 않습니다.
   2. 리스크: 리팩터링 시 회귀가 발생해도 발견이 늦습니다.
   3. 기대효과: 핵심 컴포넌트/라우팅/폼 동작이 자동으로 검증됩니다.
2. 해결 절차(권장 구성)
   1. `vitest.config.ts` 또는 `vite.config.ts`의 `test` 섹션으로 구성 통일
   2. `src/test/setup.ts`에서 `@testing-library/jest-dom`과 MSW 서버를 초기화
   3. `src/test/renderWithProviders.tsx`로 테스트 렌더 유틸 제공(라우터/쿼리/스토어 주입)
   4. `package.json`에 `test`, `test:watch`, `test:coverage` 등 스크립트 추가
3. 검증(성공 기준)
   1. `npm run test`가 로컬에서 1분 내 실행되고 안정적으로 통과합니다.

### 7.12) 12단계: E2E 테스트 표준화(Playwright)

1. 원인/리스크/기대효과
   1. 원인: CSR에서 “사용자 플로우”는 라우팅/네트워크/렌더링이 함께 동작해야 합니다.
   2. 리스크: 단위 테스트만으로는 브라우저 이슈나 통합 회귀를 놓칠 수 있습니다.
   3. 기대효과: 배포 전 핵심 시나리오 회귀를 방지합니다.
2. 해결 절차(권장 구성)
   1. `playwright.config.ts` 추가
   2. 로컬에서는 `vite preview`(또는 dev server) 기반으로 E2E 실행
   3. 기본 시나리오 1개를 템플릿에 포함
      1. 예: 홈 진입 → 라우팅 → 기본 UI 렌더 확인
3. 검증(성공 기준)
   1. `npm run e2e` 실행 시 실제 브라우저가 떠서 테스트가 통과합니다.

### 7.13) 13단계: 문서/운영 기준 정리(템플릿 사용자를 위한 “기본 규칙”)

1. 원인/리스크/기대효과
   1. 원인: 구조/규칙이 문서화되지 않으면 템플릿이 “그냥 시작 코드”로 전락합니다.
   2. 리스크: 팀마다 다른 규칙이 섞여 결국 템플릿이 깨집니다.
   3. 기대효과: 템플릿 사용자가 “왜 이렇게 해야 하는지”를 이해하고 그대로 따를 수 있습니다.
2. 해결 절차(권장 문서)
   1. 이 문서(`CSR_TEMPLATE_IMPLEMENTATION_GUIDE.md`)를 “구현자 문서”로 유지
   2. 템플릿 사용자를 위한 README(새 프로젝트 시작 절차) 업데이트 계획 수립
   3. 기존 문서와 역할 분리
      1. 품질 게이트: `CODE_QUALITY_GATE_GUIDE.md`
      2. 스타일: `STYLES_BOILERPLATE_GUIDE.md`
3. 검증(성공 기준)
   1. 신규 개발자가 문서만 보고 “설치 → 실행 → 검증 명령”을 재현합니다.

---

## 8) 최종 검증 체크리스트(성공 기준)

1. 로컬 실행
   1. `npm ci` 또는 `npm install` 후 `npm run dev`가 정상 실행됩니다.
   2. 라우팅이 동작합니다(최소 2개 라우트 + 404).
2. 품질 게이트
   1. `npm run validate`가 통과합니다.
3. 테스트
   1. `npm run test`가 통과합니다(최소 1개 테스트 포함).
   2. `VITE_ENABLE_MOCK=true`에서 목킹이 동작합니다.
4. E2E
   1. `npm run e2e`가 통과합니다(최소 1개 시나리오).
5. 운영 안정성
   1. 강제 에러 발생 시 Error Boundary 폴백 UI가 보입니다.
   2. API 에러 메시지가 사용자 친화적으로 표준화되어 있습니다.

---

## 9) 구현 후 추가 개선 후보 3가지(템플릿 품질을 더 올리는 옵션, 기본 포함 여부는 추후 결정)

1. (개발자 경험) 라우트/페이지 생성 스크립트 추가
   1. 목적: 페이지 추가를 자동화해 컨벤션 붕괴를 줄입니다.
2. (성능) 번들 분석/성능 예산 추가
   1. 목적: 번들 크기 증가를 조기에 감지합니다(템플릿이 커지는 리스크를 통제).
3. (보안/운영) 에러/성능 모니터링 연동(Sentry 등) “연결 지점”을 표준화
   1. 목적: 운영에서 발생하는 이슈를 빠르게 수집/분류합니다.
